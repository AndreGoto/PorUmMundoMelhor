<div class="container">
  <div class="row">
    <div class="col-xs-12 col-sm-6 col-sm-offset-3">
    <% if current_user.id == @job.user_id %>
      <h1>Edite a sua causa</h1>
      <%= render 'form' %>
    <% else %>
      <p>Você não pode editar esta página</p>
    <% end %>
    </div>
  </div>
</div>
